// Oh no, Timmy's filter doesn't seem to be working? Your task is to fix the FilterNumber function to remove all the numbers from the string.
const FilterNumbers = str => str.split('').join('').replace(/[0-9]/g, '')
